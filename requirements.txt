flask>=3.0
ultralytics>=8.2
opencv-python-headless>=4.8
numpy>=1.24
gunicorn>=21.0
# torch e instalado automaticamente como dependencia do ultralytics.
# Em maquina sem GPU, instala a versao CPU (suficiente para inferencia).

